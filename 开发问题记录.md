# 开发问题记录

本文档用于记录使用过程中发现的问题、解决思路和主要改动位置。Git 提交、推送等操作不记录在这里。

## 记录约定

- 每次记录包含：发现的问题、解决思路、主要改动位置。
- 只写必要信息，不粘贴大段代码。
- 后续对话中如果继续发现功能或体验问题，应追加到本文档。

## 1. 菜单栏按钮文字显示为省略号

**发现的问题**  
工具栏按钮很多，窗口宽度不够时，按钮文本被 JavaFX 压缩成 `...`，无法看清功能。

**解决思路**  
让按钮保持自身首选宽度，不再被强行压窄；工具栏外层改为横向滚动区域，空间不足时滚动查看。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 2. 缩放比例数字不更新，布局按钮看起来无效

**发现的问题**  
点击放大、缩小时，界面右侧比例数字没有同步变化；点击“自动 / 左侧 / 右侧”布局时，经常没有明显效果。

**解决思路**  
缩放后统一刷新界面比例显示；布局切换改为全局生效，不再要求先选中中心节点。顺带修正画布缩放后的可滚动区域，避免放大后边缘不好滚到。

**主要改动位置**

- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/MainFrame.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 3. 右侧结构栏占用空间，不能收起

**发现的问题**  
右侧“结构”面板一直占据画布空间，编辑大图时不方便。

**解决思路**  
给结构栏标题区增加收起按钮；收起后保留窄栏和展开按钮，画布自动获得更多空间。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/MainFrame.java`
- `src/main/java/com/example/mindmap/view/MindMapTreePanel.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 4. 原工具栏不够像 WPS，功能分布不够清晰

**发现的问题**  
所有功能挤在一条工具栏里，查找、节点、样式、布局、导出混在一起，不够直观。

**解决思路**  
参考 WPS 的 Ribbon 结构，增加“开始 / 样式 / 布局 / 导出”页签，并把命令按场景分组展示。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 5. 文件入口不明显，开始页重复放文件功能，缩放按钮位置不顺手

**发现的问题**  
左上角“文件”像普通菜单文字，不够醒目；“开始”页和“导出”页重复放了文件操作；缩放功能放在顶部工具栏，不如放在画布右下角/底部区域顺手。

**解决思路**  
把左上角文件入口改成醒目的绿色下拉按钮；“开始”页只保留查找、节点、编辑；缩放控制移到底部右侧，显示为 `- / 当前比例 / + / 适应`。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`
- `src/main/java/com/example/mindmap/view/MainFrame.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 6. 需要复杂 `.mindmap` 文件用于导入测试

**发现的问题**  
普通导图覆盖不了折叠、搜索、多层级、长文本、颜色、偏移等边界情况，测试不充分。

**解决思路**  
新增一个复杂测试文件，包含多层级、左右分支、长文本、重复关键词、折叠节点、不同颜色、手动偏移和深层链路等场景。

**主要改动位置**

- `复杂导入测试.mindmap`

## 7. 复杂导图导出图片清晰度不够

**发现的问题**  
将复杂 `.mindmap` 文件导出为图片时，文字和连线不够清晰。

**解决思路**  
导出时不再按当前画布显示比例直接截图，而是使用 2 倍高清快照；JPG 导出改用较高压缩质量。

**主要改动位置**

- `src/main/java/com/example/mindmap/service/ImageExportService.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 8. 缺少常见快捷键和鼠标滚轮缩放

**发现的问题**  
编辑导图时缺少 `Ctrl + 鼠标滚轮` 放大缩小等常见操作，使用起来不够顺手。

**解决思路**  
在画布层拦截 `Ctrl + 鼠标滚轮`，向上滚放大、向下滚缩小；补充常用键盘快捷键，包括 `Ctrl+Shift+Z` 重做、`F3 / Shift+F3` 切换搜索结果、`Esc` 取消选择。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`
- `src/main/java/com/example/mindmap/controller/MindMapController.java`

## 9. 查找替换、展开收起、框选批量样式和画布背景能力不足

**发现的问题**  
只有基础查找，没有替换；缺少一键展开/收起；批量修改样式需要先逐个选择节点；画布边界不明显，也不能设置画布背景色。

**解决思路**  
增加查找替换、`Ctrl+F`/`Ctrl+H` 快捷键、全部展开/全部收起；框选后复用批量样式修改逻辑；窗口背景改灰色、画布默认白色并显示边界，样式页增加画布背景色选择器并保存到 `.mindmap`。

**主要改动位置**

- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`
- `src/main/java/com/example/mindmap/model/MindMap.java`
- `src/main/java/com/example/mindmap/model/SelectionModel.java`
- `src/main/java/com/example/mindmap/service/MindMapFileService.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 10. 框选误触发并影响画布拖动

**发现的问题**  
普通左键框选空白区域时会干扰现有交互，出现节点跟随框选操作移动的异常；同时普通拖动画布也不顺畅。

**解决思路**  
把框选触发方式改为 `Ctrl + 鼠标左键拖动`，普通左键拖动不再进入框选逻辑，继续留给画布拖动/平移使用；框选矩形设置为不参与鼠标命中，避免它拦截后续事件。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 11. 缩放后节点与画布坐标系错位

**发现的问题**  
缩放后节点没有固定在白色画布上，而是漂到灰色背景区域，看起来像节点和画布分离。

**解决思路**  
原来节点组使用 `setScaleX/Y` 缩放，会围绕节点组自身中心缩放；画布尺寸却按左上角计算，导致坐标系不一致。改为使用以画布左上角 `(0,0)` 为锚点的 `Scale` 变换，让节点和画布使用同一个缩放原点。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 12. 节点编辑和样式操作不符合常见绘图软件习惯

**发现的问题**  
双击节点时仍然弹窗改名，不如直接在节点框内编辑；选中节点后缺少八个缩放控制点；开始菜单里缺少常见文字样式按钮；查找替换控件常驻菜单占用空间。

**解决思路**  
双击节点后在节点内部切换为文本输入框；选中节点时显示八个调整点；拖拽节点时避免按下鼠标就重绘打断拖动；扩展节点样式模型并保存字体、字号、粗斜体、下划线、删除线和对齐；查找替换改为按钮触发弹窗。

**主要改动位置**

- `src/main/java/com/example/mindmap/model/NodeStyle.java`
- `src/main/java/com/example/mindmap/model/MindNode.java`
- `src/main/java/com/example/mindmap/service/MindMapFileService.java`
- `src/main/java/com/example/mindmap/service/MindMapLayoutService.java`
- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`
- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 13. 选中节点后无法正常拖动

**发现的问题**  
选中节点后拖动不生效，或者刚开始拖动就中断。

**解决思路**  
拖动过程中不再调用整图刷新，因为刷新会销毁并重建当前节点视图，导致鼠标拖动事件链断开。改为拖动中只更新模型坐标和当前节点视图位置，松开鼠标后再统一刷新画布和记录撤销快照。

**主要改动位置**

- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 14. 点击空白处没有取消节点选中

**发现的问题**  
选中节点后，点击画布空白区域时选中框仍然保留，不符合常见编辑器交互习惯。

**解决思路**  
在画布空白区域的鼠标释放事件中处理普通左键点击：没有按 `Ctrl` 且目标是画布空白时清空选择；`Ctrl + 左键拖动` 仍然保留为框选逻辑。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 15. 画布从左上角开始且缩放不居中

**发现的问题**  
缩小画布后白色画布贴在工作区左上角，看起来不自然；继续放大缩小时，视口也更像围绕左上角变化，不符合常见绘图软件以当前视图中心缩放的习惯。

**解决思路**  
在画布外增加一层灰色居中容器，让白色画布在可视区域比画布大时自动居中；缩放刷新前记录当前视口中心对应的画布逻辑坐标，刷新后再把滚动位置恢复到这个逻辑中心，从而实现“围绕当前视图中心缩放”。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 16. 节点超出画布后缺少自动扩展

**发现的问题**  
当节点被拖到画布边缘之外时，固定画布大小不够用；原有计算主要关注右侧和下方，左侧或上方超出时没有自然补出画布空间。

**解决思路**  
按可见节点的最小/最大边界重新计算画布尺寸：右侧和下方超出时扩大宽高，左侧和上方超出时给内容增加内部偏移和安全留白，让节点仍显示在白色画布范围内。

**主要改动位置**

- `src/main/java/com/example/mindmap/service/MindMapLayoutService.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`

## 17. 导出格式缺少 PDF

**发现的问题**  
导出只支持 PNG 和 JPG，不方便作为文档提交、打印或在 PDF 阅读器中查看。

**解决思路**  
复用现有高清画布快照，转换为不透明 RGB 图像后写入单页 PDF；PDF 内部使用无损压缩嵌入图像，避免新增第三方依赖。文件菜单和“导出”页同步增加 PDF 入口。

**主要改动位置**

- `src/main/java/com/example/mindmap/service/ImageExportService.java`
- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`

## 18. 文本对齐按钮图标区分不清

**发现的问题**  
左对齐、居中对齐、右对齐按钮使用相近的文本符号，视觉上很难区分，尤其是左对齐和右对齐几乎看不出来差别。

**解决思路**  
把对齐按钮从普通文字改成小图形控件：三条横线分别按左、中、右摆放，接近常见办公软件的对齐图标；按钮 tooltip 保持不变，鼠标靠近时仍显示具体功能。

**主要改动位置**

- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`
- `src/main/resources/com/example/mindmap/styles.css`

## 19. 拖动节点时连接线不同步、滚动条闪烁、连接线缺少样式和右键菜单

**发现的问题**  
选中多个节点拖动时，节点会实时移动，但连接线停留在原处，松开鼠标后才更新；缩小画布时滚动条在临界尺寸附近闪烁；连接线不能设置颜色、粗细、实线/虚线；右键菜单功能偏少，不方便快速操作。

**解决思路**  
为连接线建立视图引用，拖动或调整节点大小时只局部更新相关曲线坐标，不触发整图刷新；画布滚动条改为固定显示，避免自动显隐导致视口尺寸反复变化；在导图模型中增加连接线颜色、粗细、虚线状态并保存到 `.mindmap`；样式页增加连接线设置；节点和空白画布分别提供右键菜单，放置节点编辑、样式、布局、视图和整体展开收起等常用操作。

**主要改动位置**

- `src/main/java/com/example/mindmap/model/MindMap.java`
- `src/main/java/com/example/mindmap/service/MindMapFileService.java`
- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`
- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`

## 20. 连线不能作为对象单独选择和批量修改

**发现的问题**  
连线样式只能全局修改，不能像节点一样单独选中某条连线，也不能批量选择几条连线后只修改它们；同时缺少直角分段的连线样式。

**解决思路**  
把每条父子连线视为独立对象，样式保存到对应子节点的“入线”样式中，并兼容旧文件的全局连线设置；选择模型增加连线选择集合，支持单击选中连线、`Ctrl + 点击` 多选连线；连接线样式按钮优先作用于已选连线，没有选中连线时才修改默认样式；新增“曲线 / 折线”两种连线形状，折线使用水平-垂直-水平的直角路径。

**主要改动位置**

- `src/main/java/com/example/mindmap/model/ConnectionStyle.java`
- `src/main/java/com/example/mindmap/model/ConnectionShape.java`
- `src/main/java/com/example/mindmap/model/MindNode.java`
- `src/main/java/com/example/mindmap/model/MindMap.java`
- `src/main/java/com/example/mindmap/model/SelectionModel.java`
- `src/main/java/com/example/mindmap/service/MindMapFileService.java`
- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/view/MindMapCanvas.java`
- `src/main/java/com/example/mindmap/view/ToolbarPanel.java`

## 21. 导出耗时较长但没有进度提示

**发现的问题**  
导出高清 PNG、JPG 或 PDF 时，如果文件较大，界面没有任何提示，用户容易误以为程序卡住。

**解决思路**  
导出前弹出模态进度窗口，先提示正在生成高清画布快照；快照完成后把文件写入放到后台任务执行，进度窗口继续显示正在写入对应格式文件，成功或失败后自动关闭并更新状态栏。

**后续修正**  
原进度弹窗清空了按钮并拦截关闭事件，导致导出完成后 `close()` 也可能无法收起，用户手动点击关闭也无效。改为提供“后台运行”按钮允许用户隐藏弹窗，并在导出结束时使用 `hide()` 收起窗口。

**主要改动位置**

- `src/main/java/com/example/mindmap/controller/MindMapController.java`
- `src/main/java/com/example/mindmap/service/ImageExportService.java`
